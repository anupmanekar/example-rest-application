spring:
  jpa:
    hibernate:
      ddl-auto: none

  datasource:
    url: jdbc:mysql://localhost:3306/example_db?zeroDateTimeBehavior=convertToNull
    username: springuser
    password: admin

server:
  port: 8080